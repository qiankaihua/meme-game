import {Status} from "./Constants";
import PathFinderNodeFast from "../../AStar/PathFinderNode";

let util = require("util");

export default class Loop extends PathFinderNodeFast {
    private a: number;
    constructor () {
        super();
        // to do
    }
}
